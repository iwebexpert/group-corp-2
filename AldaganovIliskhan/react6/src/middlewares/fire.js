// import { fireChat } from "../actions/chats";

// export const fire = (store) => (next) => (action) => {
//   switch (action.type) {
//     case "SEND_MESSAGE": {

//     }
//   }
//   return next(action);
// };
