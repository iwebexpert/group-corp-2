<div>
    <h3>Ваши задачи</h3>
    <div class="task">
        {{#each tasks}}
        <div class="taskOne">
        {{#if this.check}}
            <a href="/tasks/{{@key}}"><input class="checkbox" type="checkbox" id={{@key}} checked>{{this.taskname}}</a><Br>
        {{else}}
            <a href="/tasks/{{@key}}"><input class="checkbox" type="checkbox" id={{@key}}>{{this.taskname}}</a><Br>
        {{/if}}
        <button id={{@key}} class="btn btn-danger">Удалить задачу</button>
        </div>
        {{else}}
        <p class="taskEmpty">Список задач пуст!</p>
        
        {{/each}}
    </div>
</div>
<div>
    <h3>Добавить задачу</h3>
    <form action="/tasks" method="POST">
    <div>
        <input class="form-control" type="text" name="taskname" placeholder="Название задачи">
    </div>
    <div>
        <textarea class="form-control" type="text" name="comment" placeholder="Комментарий"></textarea>
    </div>
    <div>
        <input class="btn btn-success addBtn" type="submit" value="Добавить задачу">
    </div>
    </form>
</div>