import React from "react";
import SearchMessPanel from "./SearchMessPanel";

export default () => {
    return (
        <div className={'selectDialogPanel'}>
            <SearchMessPanel/>
        </div>
    );
}
