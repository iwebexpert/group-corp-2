import {takeEvery, put, call, select, throttle} from 'redux-saga/effects';
import actionTypes from "../actionTypes";

export function* sagaWatcher() {
   //yield takeEvery(actionTypes.UPDATE_WS_CONNECTION_STATUS, sagaScrollWorker);
}


